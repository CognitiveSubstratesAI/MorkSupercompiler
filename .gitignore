# Julia build artifacts
*.jl.cov
*.jl.*.cov
*.jl.mem
/Manifest.toml
/.julia/

# Claude Code local settings
.claude/

# ⚠️  NEVER commit files containing hardcoded local paths.
# Install hook: git config core.hooksPath .githooks
# Secrets / credentials — never commit
.env
.env.*
*.key
*.pem
*.p12
*.pfx
.netrc
secrets.*
*.secret
credentials.json
